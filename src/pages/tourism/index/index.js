// import { VisualApi } from '../../../api/Index'
import Base from '../../base/Index'

export default class extends Base {
  /**
   *取旅游线路-近两日客流量
   */
  touristRouteTwoDay (param) {
    // VisualApi.touristRouteTwoDay(param).then(res => {
    //   if (res.data) {
    //     this.vm.twoDay = res.data[0].items_count
    //   }
    // })
  }
}
